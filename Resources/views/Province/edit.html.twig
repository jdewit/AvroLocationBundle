<form id="editFormModal" class="horizontal-form modal hide" action="{{ formAction }}" data-backdrop="true" method="POST">
    <div class="modal-header">
        <button type="button" class="close" data-dismiss="modal">×</button>
        <h3>Edit Province</h3>
    </div>
    <div class="modal-body">
        {% include 'AvroLocationBundle:Province:form.html.twig' %}
    </div>
    <div class="modal-footer">
        <button href="#" class="btn btn-primary btn-large" type="submit"><i class="icon-ok icon-white"></i> Update Province</button>
        <a href="#" class="btn close-modal" data-dismiss="modal"><i class="icon-remove"></i>Close</a>
        {% if province.isDeleted %}
            <a class="btn confirm btn-success" href="{{ path('avro_user_province_restore', { 'id': province.id }) }}" title="Are you sure you want to restore this Province?"><i class="icon-refresh icon-white"></i>Restore</a>
        {% else %}
            <a class="btn confirm btn-danger" href="{{ path('avro_user_province_delete', { 'id': province.id }) }}" title="Are you sure you want to delete this Province?"><i class="icon-trash icon-white"></i>Delete</a>
        {% endif %}
    </div>
</form>
