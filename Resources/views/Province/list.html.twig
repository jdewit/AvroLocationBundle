{% extends 'AvroLocationBundle::layout.html.twig' %}
{% block content %}
    <div class="page-header">
        <h1>
            Province List
            <a href="#provinceForm" class="btn btn-success btn-large pull-right" data-toggle="modal"><i class="icon-plus icon-white"></i> New Province</a>
        </h1>
    </div>
    {% include 'AvroLocationBundle:Province:table.html.twig' %}
    {% if province.id %}
        {% include 'AvroLocationBundle:Province:edit.html.twig' %}
    {% else %}
        {% include 'AvroLocationBundle:Province:new.html.twig' %}
    {% endif %}
{% endblock %}
{% block js %}
{% if app.request.method == 'POST' %}
    $('#cityForm').modal('show');
{% endif %}
{% endblock %}
